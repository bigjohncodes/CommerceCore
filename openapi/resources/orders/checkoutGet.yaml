get:
    tags:
        - orders
    summary: Lấy checkout info
    description:
    operationId: orders_checkout_get
    security:
        - BearerAuth: []
    parameters:
        - $ref: "../../parameters/session_checkout_id.yaml"

    responses:
        "200":
            description:
        "400":
            description:
        "422":
            description: Sai các trường thông tin
